// Centralized configuration for the extension.
// Change this one value to point the whole extension at a different
// backend (e.g. when you deploy the FastAPI server somewhere other
// than localhost).
const CONFIG = {   
    API_BASE_URL: "http://127.0.0.1:8000"
};